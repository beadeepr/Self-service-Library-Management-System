# 无人值守图书馆管理系统 — 需求文档

| 项目名称 | 无人值守图书馆管理系统 |
| --- | --- |
| 文档版本 | V1.0 |
| 编写日期 | 2026-09-09 |
| 文档状态 | 初稿（待评审） |

---

## 1. 引言

### 1.1 编写目的
本需求文档旨在明确"无人值守图书馆管理系统"的功能需求、非功能需求（质量属性）与约束条件，作为后续**架构设计、开发、测试与验收**的依据。本文档面向开发团队、项目评审老师及相关干系人。

### 1.2 项目背景
传统图书馆依赖人工值守，存在开放时间受限、借还流程繁琐、人工盘点成本高、夜间或节假日无法服务等问题。本项目设计一套**无人值守图书馆管理系统**，通过**物联网感知设备 + 身份识别 + 自动化业务**，实现读者在非人工值守时段也能自助完成入馆、借还、预约、续借等操作，并保证图书与设备安全。

### 1.3 术语定义
| 术语 | 说明 |
| --- | --- |
| RFID | 无线射频识别，用于图书定位、盘点与防盗 |
| 自助终端 | 读者自助借还书、缴费、查询的交互设备 |
| 门禁 | 控制读者进出馆的闸机设备 |
| 读者 | 使用本系统借阅图书的个人用户 |
| 管理员 | 负责馆藏、读者、设备与报表管理的后台人员 |
| 图书状态 | 在馆 / 借出 / 预约 / 下架 / 遗失 |

### 1.4 读者对象
- 普通读者（持证/注册用户）
- 图书管理员
- 系统运维人员 / 设备维护人员
- 馆方管理者（关心统计报表、成本与安全）

---

## 2. 系统总体描述

### 2.1 系统目标
1. 实现 7×24 小时无人值守自助服务，突破人工值守时间限制。
2. 自动化借还、续借、预约、罚款计算，降低人工成本与出错率。
3. 通过 RFID 实现图书定位、自动盘点与防盗，提升管理效率。
4. 提供多端接入（自助终端、WEB），提升读者体验。
5. 保障系统在无人场景下的安全性、稳定性与可维护性。

### 2.2 系统范围
- **业务范围**：读者管理、图书管理、自助借还、预约取书、逾期罚款、智能环境与安防、运营报表。
- **不在本期范围**：图书采编（采购、编目）的自助化、财务记账、用户社交社区等（可作为二期扩展）。

### 2.3 运行环境
- **读者端**：WEB 浏览器（响应式，适配 PC 与移动设备浏览器）、自助终端触摸屏（同样基于 Web）。
- **管理端**：后台管理 Web 系统（PC 浏览器，基于 Django Admin 扩展）。
- **服务端**：Django 应用服务器 + 数据库服务器，支持容器化部署。
- **物联网设备**：RFID 读写器、门禁闸机、人脸识别终端、温湿度传感器、监控摄像头（以模拟/仿真为主）。

---

## 3. 功能需求

### 3.1 读者/用户管理

**FR-01 读者注册**
- 读者通过手机号注册，设置密码；支持图形验证码与短信验证码（可选）。
- 业务规则：同一手机号仅可注册一个账号；验证码有效期 5 分钟，错误 5 次后锁定。

**FR-02 读者登录**
- 支持账号密码登录 / 短信验证码登录 / 扫码登录。
- 业务规则：连续 5 次密码错误锁定账号 15 分钟。

**FR-03 实名认证**
- 读者绑定身份证（可对接第三方实名认证）。
- 业务规则：实名认证后可提升借阅权限（借阅上限、押金减免）；一人一证。

**FR-04 读者信息维护**
- 支持修改手机号、密码、头像、联系方式。

**FR-05 读者等级与信用**
- 系统按借阅行为自动核算信用分，划分等级（正常 / 受限 / 失信）。
- 业务规则：等级不同，借阅上限、借期、押金不同；失信用户冻结借阅。

### 3.2 图书管理（馆藏）

**FR-06 图书入库**
- 录入 ISBN、书名、作者、出版社、分类号、索书号、价格、单价、馆藏数量、封面图片。
- 业务规则：ISBN 唯一；库存数量 >= 0；入库时同步生成 RFID 标签。

**FR-07 图书信息维护**
- 支持编辑、下架、报废、借出/在馆状态管理。

**FR-08 图书检索**
- 支持按书名 / ISBN / 作者 / 分类号进行精确与模糊检索，支持组合条件（多字段 AND/OR）。
- 业务规则：展示图书状态（在馆、借出、可预约）及可借数量。

**FR-09 分类管理**
- 管理员维护图书分类树（如中图法）。

**FR-10 馆藏统计**
- 提供在馆量、借出量、热门图书排行、分类占比等统计。

### 3.3 自助借还书

**FR-11 自助借书**
- 读者在自助终端通过人脸 / 刷卡 / 扫码完成身份识别，将图书置于识别区，系统校验后出借。
- 业务规则（关键规则）：
  - 仅允许合法且信用正常的读者借书；
  - 当前借阅数量不得超过该读者等级上限；
  - 存在逾期或欠费时禁止借书；
  - 借书成功后更新图书状态为"借出"，并记录借阅人、借出时间、应还时间。

**FR-12 自助还书**
- 读者将图书放入还书口，RFID 识别归还。
- 业务规则：归还后自动判定是否超期并计算罚款；图书状态更新为"在馆"；异常图书（损坏）转入待处理。

**FR-13 续借**
- 读者可在应还日期前在线续借。
- 业务规则（关键规则）：每册图书最多续借 1 次（可配置）；逾期图书不可续借；续借后应还日期顺延。

**FR-14 借阅历史**
- 读者查看个人借阅记录、当前在借、历史逾期、罚款记录。

### 3.4 预约与取书

**FR-15 图书预约**
- 当图书全部借出时，读者可预约。
- 业务规则（关键规则）：每人最多同时预约 5 册（可配置）；预约需提前支付或免押（视配置）。

**FR-16 取书管理**
- 图书归还后被预约读者取走；系统触发消息通知。
- 业务规则（关键规则）：预约保留期 2 天（可配置），超时自动释放并顺延给下一位预约者。

**FR-17 预约提醒**
- 通过站内信 / 短信 / 推送通知读者图书到馆。

### 3.5 逾期与罚款

**FR-18 逾期计算**
- 系统每日定时任务自动计算逾期天数与罚款金额。
- 业务规则（关键规则）：逾期按天计费，费率可配置；节假日可配置是否顺延。

**FR-19 罚款缴纳**
- 支持在线支付（对接第三方支付 / 沙箱模拟）及线下处理。
- 业务规则：缴纳成功后更新读者欠费状态；未缴清欠费者限制借书。

**FR-20 信用管理**
- 逾期 / 欠费 / 损坏图书影响信用分。

### 3.6 无人值守特色功能

**FR-21 自助进馆与门禁**
- 读者通过人脸 / 刷卡 / 扫码验证后自动开门进馆。
- 业务规则：非注册或失信读者禁止入馆；记录进出馆时间与人流量。

**FR-22 智能环境控制**
- 传感器采集温湿度、光照；联动自动调节空调与照明。
- 业务规则：温湿度超过阈值触发告警并自动开关设备。

**FR-23 安防监控**
- 摄像监控、烟雾/火灾探测、异常事件录像与告警。

**FR-24 RFID 定位与盘点**
- 实时定位图书在馆位置，检测错架。
- 业务规则：支持按楼层/书架定时或手动盘点，输出盘亏/盘盈报告。

**FR-25 异常与告警**
- 未借出图书离馆报警、设备离线告警、设备故障通知管理员。

**FR-26 在馆人数统计**
- 大屏展示实时在馆人数、座位空闲情况。

### 3.7 管理后台

**FR-27 借还 / 订单管理**
- 管理员查询、催还、手工登记借还、处理异常图书。

**FR-28 读者管理**
- 冻结/解冻、重置密码、调整信用等级、添加/扣除押金。

**FR-29 图书调配**
- 馆际调拨、下架、报废、报损。

**FR-30 设备监控**
- 展示门禁、自助终端、传感器、摄像头状态；远程开关机。

**FR-31 报表与统计**
- 借阅量趋势、收入（罚款/押金）、热门图书、读者活跃度、设备利用率。

**FR-32 消息与公告**
- 多场景通知（预约到馆、即将到期、逾期、设备告警）；发布公告。

---

## 4. 非功能需求（质量属性）

### 4.1 性能
- 常见业务接口（登录、查询、借还）响应时间 **≤ 500ms**（95% 请求）。
- 系统支持 **≥ 500 并发读者**同时操作（按规模可调）。
- 图书检索在 10 万条记录下响应 ≤ 2s。

### 4.2 可用性
- 系统可用性目标 **99.9%**（无人值守场景要求 7×24 稳定运行）。
- 故障自动恢复：核心服务（借还、门禁）支持主备切换。
- 设备离线时提供降级方案（如离线借书、事后补录）。

### 4.3 安全性
- 读者身份与隐私：密码加密存储（如哈希+盐）；敏感信息（身份证、手机号）加密传输。
- 权限控制：读者 / 管理员 / 运维角色分离，基于角色的访问控制（RBAC）。
- 支付安全：对接第三方支付需签名校验，防重放、防篡改。
- 接口安全：登录鉴权（Token / JWT）、接口频率限制、防 SQL 注入与 XSS。
- 审计日志：关键操作（借还、缴费、权限变更）留痕。

### 4.4 可靠性
- 交易一致性：借还、续借涉及库存与借阅记录的原子操作，防止数据不一致。
- 数据备份：数据库每日备份，支持故障恢复。

### 4.5 可维护性
- 模块化 / 分层设计，便于独立升级。
- 提供操作日志、错误日志与告警，便于排障。
- 设备接入采用可扩展接口，便于新增硬件。

### 4.6 易用性
- 自助终端界面简洁，引导清晰；核心操作 3 步内完成。
- 支持无障碍与中文界面，字体/按钮尺寸适合触摸操作。

### 4.7 可扩展性
- 支持扩展新业务模块（座位预约、推荐、多分馆）。
- 支持横向扩展部署（无状态服务 + 负载均衡）。

---

## 5. 约束与限制条件

### 5.1 技术约束
- 技术栈：后端 **Django（Python）**，前端 **Vue 3 + Element Plus + Vite**，数据库 **MySQL（主库）+ Redis（缓存）**，物联通信 **MQTT（EMQX，模拟设备）**。
- 客户端形态：基于 Web（B/S）架构，支持响应式；**无独立移动 App / 小程序**。
- 部署环境：内网 + 公网网关，支持容器化（Docker）。
- 物联网接口：RFID（ISO/IEC 18000-6C）、人脸识别 SDK、门禁闸机协议（以模拟/仿真实现）。

### 5.2 业务约束
- 借阅规则参数化，需支持管理员配置（借阅上限、借期、续借次数、预约上限、罚款费率等）。
- 实名认证合规：遵守个人信息保护相关法规，最小化收集与明确告知。
- 支付需接入合规第三方支付渠道（本期以沙箱/模拟支付实现）。

### 5.3 资源与成本约束
- 项目周期与团队规模受限（按课设/毕设节奏）。
- 硬件设备（RFID、门禁、摄像头）以**模拟/仿真**方式实现，重点做软件系统与业务流程。
- 无需真实支付接口（可用沙箱/模拟支付）。

### 5.4 验收约束
- 需提供可运行演示（含自助终端模拟、后台管理、读者 Web 端）。
- 关键业务规则需通过单元测试与用例验证。

---

## 6. 用例概览（供参考）

| 用例 | 参与者 | 描述 |
| --- | --- | --- |
| UC-01 自助借书 | 读者 | 识别身份→校验资格→出借 |
| UC-02 自助还书 | 读者 | 识别图书→校验逾期→更新库存 |
| UC-03 续借 | 读者 | 在线续借，校验次数与状态 |
| UC-04 预约图书 | 读者 | 预约已借出图书 |
| UC-05 取书 | 读者 | 到馆取预约图书 |
| UC-06 缴纳罚款 | 读者 | 在线支付逾期罚款 |
| UC-07 图书入库 | 管理员 | 录入并生成 RFID 标签 |
| UC-08 图书盘点 | 管理员 | 触发自动盘点生成报告 |
| UC-09 门禁进馆 | 读者 | 人脸/刷卡核验进馆 |
| UC-10 设备告警 | 系统 | 设备离线自动通知运维 |

---

## 7. 未来扩展（可选）
- 座位 / 自习室预约与签到。
- 基于借阅历史的个性化图书推荐。
- 书评、评分、读书打卡社区。
- 多馆 / 分馆馆际互借。
- 数据可视化大屏。

---

*文档结束*
